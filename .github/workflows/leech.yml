name: Syzo Leech Worker
on:
  repository_dispatch:
    types: [start-leech]

jobs:
  leech-job:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Telegram (Process Start)
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TOKEN_BOT }}/sendMessage" \
          -d "chat_id=${{ secrets.CHAT_ID }}&text=⚡ *Worker Started!*%0A sedang men-download dari SourceForge...&parse_mode=Markdown"

      - name: Download File from SourceForge
        run: |
          sudo apt-get install -y aria2
          wget -U "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
               --header="Referer: https://sourceforge.net/" \
               -O leech_file \
               "${{ github.event.client_payload.url }}"

      - name: Upload to GoFile
        run: |
          echo "🚀 Memulai upload anonim ke GoFile..."
          RESPONSE=$(curl -s -F "file=@leech_file" https://upload.gofile.io/uploadfile)
          
          echo "Respon Server: $RESPONSE"
          
          # Mengambil link download menggunakan jq
          DOWNLOAD_URL=$(echo "$RESPONSE" | jq -r '.data.downloadPage // empty')
          
          if [[ -n "$DOWNLOAD_URL" && "$DOWNLOAD_URL" != "null" ]]; then
            echo "DOWNLOAD_URL=$DOWNLOAD_URL" >> $GITHUB_ENV
            echo "✅ Berhasil! Link: $DOWNLOAD_URL"
          else
            echo "❌ Gagal mendapatkan link. Respon server: $RESPONSE"
            exit 1
          fi

      - name: Notify Telegram (Success)
        if: success()
        run: |
          # Mengirim pesan sukses ke group
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TOKEN_BOT }}/sendMessage" \
            -d chat_id="${{ secrets.CHAT_ID }}" \
            -d parse_mode="HTML" \
            -d text="<b>✅ Leech Berhasil!</b>%0A%0A🔗 <b>Link Download:</b> ${{ env.DOWNLOAD_URL }}"
