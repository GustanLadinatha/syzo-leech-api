name: Syzo Leech Worker
on:
  repository_dispatch:
    types: [start-leech]

jobs:
  leech-job:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Telegram (Process Start)
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TOKEN_BOT }}/sendMessage" \
          -d "chat_id=${{ secrets.CHAT_ID }}&text=⚡ *Worker Started!*%0A sedang men-download dari SourceForge...&parse_mode=Markdown"

      - name: Download File (Multi-Connection Speed)
        run: |
          sudo apt-get install -y aria2
          URL="${{ github.event.client_payload.url }}"
          
          # MENGGUNAKAN ARIA2C UNTUK SPEED MAKSIMAL
          # -x16: Menggunakan 16 koneksi ke server
          # -s16: Membagi file jadi 16 bagian untuk didownload barengan
          aria2c -x16 -s16 -k1M --user-agent="Mozilla/5.0" --console-log-level=error --summary-interval=0 "$URL"
          
          # Ambil nama file asli
          RAW_FILENAME=$(ls -p | grep -v / | head -n 1)
          
          # Bersihkan nama file
          CLEAN_FILENAME=$(echo "$RAW_FILENAME" | cut -d'!' -f1 | cut -d'?' -f1)
          
          if [ "$RAW_FILENAME" != "$CLEAN_FILENAME" ]; then
            mv "$RAW_FILENAME" "$CLEAN_FILENAME"
          fi
          
          # HITUNG HASH MD5
          FILE_MD5=$(md5sum "$CLEAN_FILENAME" | awk '{ print $1 }')
          echo "MD5_HASH=$FILE_MD5" >> $GITHUB_ENV
          
          echo "FILENAME=$CLEAN_FILENAME" >> $GITHUB_ENV
          echo "✅ Berhasil download (Aria2c) & bersihkan: $CLEAN_FILENAME"

      - name: Upload to GoFile
        run: |
          echo "🚀 Memulai upload file: ${{ env.FILENAME }}"
          RESPONSE=$(curl -s -F "file=@${{ env.FILENAME }}" https://upload.gofile.io/uploadfile)
          DOWNLOAD_URL=$(echo "$RESPONSE" | jq -r '.data.downloadPage // empty')
          
          if [[ -n "$DOWNLOAD_URL" && "$DOWNLOAD_URL" != "null" ]]; then
            echo "DOWNLOAD_URL=$DOWNLOAD_URL" >> $GITHUB_ENV
          else
            echo "❌ Gagal upload. Respon: $RESPONSE"
            exit 1
          fi

      - name: Notify Telegram (Success)
        if: success()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TOKEN_BOT }}/sendMessage" \
            -d chat_id="${{ secrets.CHAT_ID }}" \
            -d parse_mode="HTML" \
            -d text="<b>✅ Leech Berhasil!</b>%0A%0A📄 <b>File:</b> ${{ env.FILENAME }}%0A🔑 <b>MD5:</b> <code>${{ env.MD5_HASH }}</code>%0A🔗 <b>Link:</b> ${{ env.DOWNLOAD_URL }}"
