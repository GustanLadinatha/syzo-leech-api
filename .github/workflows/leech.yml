name: Syzo Leech Worker
on:
  repository_dispatch:
    types: [start-leech]

jobs:
  leech-job:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Telegram (Process Start)
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELE_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELE_ID }}&text=⚡ *Worker Started!*%0A sedang men-download dari SourceForge...&parse_mode=Markdown"

      - name: Download File from SourceForge
        run: |
          sudo apt-get install -y aria2
          # Mengambil URL dari kiriman Vercel
          URL="${{ github.event.client_payload.url }}"
          wget -U "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
               --header="Referer: https://sourceforge.net/" \
               -O leech_file \
               "${{ github.event.client_payload.url }}"

      - name: Upload to GoFile
        id: upload
        run: |
          # 1. Dapatkan server terbaik secara dinamis
          SERVER=$(curl -s https://api.gofile.io/getServer | jq -r '.data.server')
          
          # 2. Upload file
          echo "Uploading to server: $SERVER"
          RESPONSE=$(curl -F "file=@leech_file" "https://${SERVER}.gofile.io/uploadFile")
          
          # 3. Ambil Download Page URL
          DOWNLOAD_URL=$(echo $RESPONSE | jq -r '.data.downloadPage')
          echo "DOWNLOAD_URL=$DOWNLOAD_URL" >> $GITHUB_ENV
          echo "File uploaded successfully: $DOWNLOAD_URL"
          
      - name: Notify Telegram (Finished)
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELE_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELE_ID }}&text=✅ *Leech Berhasil!*%0A%0A🔗 Link Download: ${{ env.FINAL_LINK }}&parse_mode=Markdown"
